import React from 'react';

const SliderItem = ({key,title,text,urlVideo}) => {
    return (
        <div className="item" key={key}  style=`background-image: url()`>


        </div>
    );
};

export  default  SliderItem;
